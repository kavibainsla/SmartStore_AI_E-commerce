# SmartStore AI — Frontend

React + Vite + Tailwind CSS admin dashboard.

## Setup

```bash
npm install
cp .env.example .env
npm run dev
```

App runs at `http://localhost:5173`

## Build

```bash
npm run build
npm run preview
```
